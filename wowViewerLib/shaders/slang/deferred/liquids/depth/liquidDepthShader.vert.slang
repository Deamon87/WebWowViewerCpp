// Depth-only liquid pass vertex shader.
// The forward liquid vertex shader is a complete entry point on its own
// (carries #version, import glsl and [shader("vertex")] main), so just include it.
#include "../forward/liquidShader.vert.slang"
